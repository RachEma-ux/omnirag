apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-omnirag
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8100
      protocol: TCP
      name: http
  selector:
    app: omnirag
